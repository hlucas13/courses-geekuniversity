// entrada:
let p1 = 7;
let p2 = 9.5;
let p3 = 9;
let p4 = 8;

// processamento:
// const sumScores = p1 + p2 + p3 + p4;
// const average = (sumScores / 4).toFixed(2);

// ou:
const average = ((p1 + p2 + p3 + p4) / 4).toFixed(2);

// saída:
console.log('Média final é = ' + average);
